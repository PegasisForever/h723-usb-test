{
    // See https://go.microsoft.com/fwlink/?LinkId=733558 
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "options": {
        "env": {
            "DEFMT_LOG": "debug"
        }
    },
    "tasks": [
        {
            "label": "Flash",
            "command": "cargo",
            "args": [
                "embed"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "isBackground": true
        },
    ]
}